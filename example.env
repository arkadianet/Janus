# Copy to .env if you need these. Janus never requires a token to scan or
# browse a local catalogue.

# Hugging Face token — gated models and higher rate limits for radar/fetch only
# HF_TOKEN=

# Optional override for the writable fetch root (otherwise set via `janus root add --kind fetch`)
# JANUS_FETCH_ROOT=
